console.log('it is console log.');

console.info('console-info');

console.warn('do not come here.');

console.clear() // clear(); -> all clear hoye jabe

console.error('wrong password.');


// monitor(VariableName) -> only works on browser console. & off korar jonne unmonitor(variableName)

/*
browser e jodi likhi--
----------
const phonePlus = ${phone-plus}
-> button
monitorEvents(phonePlus); // ei button e joto event kora hoisy oi sob e monitor korbe. 

-->> unmonitorEvents(variable) // unmonitor hoye jabe

-->> monitorEvents() er moddhy aro pera meter dewa jay --- like--
monitorEvents(variablename, ['eventName', 'anotherEventName']);
*/

console.count('function k koto ber cl kora hoisy seta count kore dibe')

// chorme console api & utilities ---search about this