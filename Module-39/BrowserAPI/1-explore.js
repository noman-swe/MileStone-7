const name = 15;
const student = {
    name: 'Jolil', friend: 'borsha', job: 'business',
    makeMovie: function (name) {
        console.log(name);
    }
}
document.getElementById('name');

// it will have an error because of it is not connected with the browser.